/// <reference types="node" />
import { ILyric } from './declare';
/**
 * 解密QRC歌词，得到歌词XML文本
 * @param buffer 歌词内容，可以从.qrc文件读取，也可以通过QQ音乐API下载
 * @returns 解密后的歌词，解密失败返回null
 */
export declare function decrypt(buffer: Buffer): string | null;
/** QRC歌词编码类型 */
export declare enum EncryptType {
    /** 新版歌词 */
    QRC = 0,
    /** 旧版歌词 */
    OLD_QRC = 1
}
/**
 * 编码歌词
 * @param content 歌词的XML内容
 * @param type 歌词类型，默认为新版的歌词
 */
export declare function encrypt(content: string | Buffer, type?: EncryptType): Buffer;
/**
 * 解析QRC歌词
 * @param qrcText QRC歌词文本内容(解码后的XML格式)
 */
export declare function parse(qrcText: string): import("./declare").IKaraOKLyric;
/**
 * 将歌词转换为XML文本
 * @param lyric 歌词(只能是卡拉OK歌词)
 */
export declare function stringify(lyric: ILyric): string;
